# Quality Bar

- Never use em-dashes `—`. Prefer `.` `,` `;` instead.

- All references should be linked and clickable links. Never use `§` symbol for references.

- For any changes you should ask users permission. Do not make changes in the background without users getting notified.

- The vocabulary should be as simple as possible. Same for wording. Like "Anchor" use simpler synonyms.

- All references should be linked and clickable.

- Avoid using wording that suggests it is written by AI. Like "Per user direction...".

- Headers should not have long description in them.

- Always prefer full sentences and avoid splitting sentences or attaching half sentences with `;` and em-dashes.
